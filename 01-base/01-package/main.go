package main

// `main` is a special package where it's expected to have a `main func`
// other files will have the package same as the folder
// all files in the same folder must have the same package

// variables in other files in the same folder are visible by the main file

func main() {
	println(a)
}
